<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.seclass.crypto6300">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PenalActivity"></activity>
        <activity android:name=".PopScoreActivity" />
        <activity android:name=".ViewCryptogramActivity" />
        <activity android:name=".PopActivity" />
        <activity android:name=".ViewStatisticsActivity" />
        <activity android:name=".ViewScoresActivity" />
        <activity android:name=".UserRegistrationActivity" />
        <activity android:name=".UserLoginActivity" />
        <activity android:name=".SolveCryptogramActivity" />
        <activity android:name=".MainMenuPlayerActivity" />
        <activity android:name=".MainMenuAdminActivity" />
        <activity android:name=".CreateCryptogramActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>